# 缩小 build context：排除构建无关 / 大体积 / 敏感 / 运行时数据。
# Windows Docker Desktop 经 WSL2 传输上下文，.venv 等大目录会拖慢构建，必须排除。

# Python 虚拟环境与缓存
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# 版本控制元数据
.git/
.gitignore
.gitattributes

# 环境变量（含密钥；经 compose env_file 注入容器，镜像不需要）
.env
.env.*

# 部署编排文件（构建用不到）
docker-compose*.yml
Dockerfile
.dockerignore
deploy.sh
deploy.ps1
sandbox.toml

# 运行时数据（共享磁盘 / 产物；绝不进镜像）
workspace/
.deepagent/
.artifacts/
data/

# 日志 / 编辑器 / 操作系统
*.log
.cache/
tests/
.idea/
.vscode/
.DS_Store
Thumbs.db
